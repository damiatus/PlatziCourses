const countries = {MX: 'Mexico', CO: 'Colombia', CL: 'Chile', PE: 'Peru'};

console.log(Object.values(countries));

//Retorna solo los valores y no las llaves. Las llaves son MX, CO, CL y PE. Es el mnómbre del valoor